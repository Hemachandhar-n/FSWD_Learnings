// syntax
// in this function doesn't need the add the term "function" simplay you can add the variable
// (parameters) => {
//    statement
// }

// example

let add =(a,b,c) => {console.log("get the output vaule is = "+(a+b+c))}
add(20,30,40)